# BI 顶部提效与价值区

- 状态：已合入 main，并于 2026-09-21 部署 NeoFlow；技术发布验证通过，登录后业务验收范围见下文。
- 分支：codex/bi-efficiency-value；直接使用既有工作目录，无新建 worktree。
- 范围：SOP、动作分析、代码评审前端、专利、硬件门户与 BI Center。各应用先增加顶部提效区，中心再复用；保留全部旧卡片、图表与查询流程。
- 跨项目原因：业务指标来自各应用，中心只是展示入口；六个仓库分别发布到 NeoFlow 既有服务，先应用后中心。不迁移运行数据，不修改数据库结构。
- 口径：真实业务量与估算节时分开；缺少人工基线或 AI 后人工投入时显示“待标定”，不使用固定节时常数。质量趋势不声称为 AI 因果效果。
- 非目标：不新增专利提交率、代码问题采纳/修复率等尚无业务闭环指标；不改人员组织、权限、积分、既有同步和数据库结构。
- 验收：新增区位于旧图表/指标上方；加载失败与无数据不能伪装成节时为 0；原筛选与图表不移除；相关语法、单测/契约检查通过。
- 回滚：撤销本任务涉及文件即可；无数据库迁移。
- 用户原有未跟踪/未提交文件保持不变；SOP 本地已有提交继续保留。

## 交付记录

- 本地基线 HEAD：3deb11d1adb46d9a016ba6581e7bfdec4162371f。
- 实现：DashboardPage 顶部新增独立提效组件，复用已有网表对比/分析结果记录量；明确点击/INFO 快照不是有效任务，原 KPI、图表及明细保持。
- 验证：组件测试 3 项通过；TypeScript 检查及 npm run build 通过。构建仅有已有资源/包体积提示。新生成的三份 vendor 目录移至系统临时目录 bi-efficiency-build-4dd52308c16441db9402520e95de78bb，保留可恢复副本，未混入交付。
- 关键边界：仅完成展示与业务参考量，节时仍为“待标定”；未新增样本录入、持久化标定参数或自动节时估算流程。
- 正式验证：正式原生页面实际显示已保存网表对比结果 356、分析结果 90（2026-09-21 验证时），顶部区域及原 KPI/图表正常，浏览器无新增错误。只构建/更新 web 并校验重载既有 gateway；未运行全栈数据库修复流程。两份既有 .env.bak 文件保留。
- 发布顺序：先同步并部署各应用改动，再部署中心；逐项检查原图表与筛选、空数据/失败状态、原生 BI 权限、中心嵌入及打开原站回退入口。
