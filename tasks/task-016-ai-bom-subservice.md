# Task 016 — AI BOM 独立子服务接入

用户已授权本地整理、开发、Git 推送和 NeoFlow 部署。
新仓库 `Max0314/ai-bom`，路径 `/neo_hardware/material-match/`。

门户改动仅包括：首页入口/使用统计、专用 Docker 网络、网关代理、物料只读投影、
内部会话校验可选 fresh=1。现有调用默认保持原行为，不修改物料内容、宜搭同步或报告审核服务。

新服务处理：服务端模型密钥/提示词管理员配置、用户任务隔离、MySQL 持久化、OSS 文件归档、
TXT 坐标、组合位号及最终物料封装校验。详细实现见 ai-bom 仓库。

验收：门户现有测试与构建；独立服务权限、存储失败、冲突处理、TXT 与表格回归；
部署后运行 migration/check-stack.sh，检查新旧入口、健康、错误日志。
回滚时先撤回新卡片/路由，再恢复本次部署前门户提交，停止新服务；保留其数据库和 OSS。

## 已完成的上线验收

- 门户运行代码：`e12bcdd`；独立服务镜像：`ai-bom:7689c75`。
- 上线前门户基线：`42c3c4a`。测试报告审核服务未重新部署，保持原有运行版本。
- 本地：门户 Python 编译通过；htmlsystm 68 项、聊天室 backend 26 项测试通过；门户前端构建通过。
- 新服务：6 项后端测试通过；TXT / Excel / CSV、组合位号、冲突物料、65 项分批 AI 和报告追溯测试通过；前端构建通过。
- 浏览器模拟接口验收完成导入、五个 AI 步骤、人工确认、坐标导入、导出和任务恢复；真实模型另经线上验证。
- 公网 HTTPS 验收通过：复用门户登录、匿名拒绝、管理员配置限制、CSRF Cookie 路径、任务和文件跨用户隔离、保存恢复及版本冲突保护。
- 线上 OSS 上传及下载摘要一致；真实 AI 流式及结构化关键词响应成功。合成任务、文件和临时会话已清理。
- 门户首页、物料库和报告审核入口返回正常；`migration/check-stack.sh` 通过；门户四容器及新服务健康，近期相关日志无 traceback。
- 物料只读快照来自 72 个库，去重后 4,314 条；19 个物料代码存在来源字段差异。保留来源版本供查阅，排除自动选型；不修改原库。
- 用户提供 TXT 样例解析 1,248 个坐标；481 个可选 `m` 标记保留原值，不推断其板面含义。

本次线上模型验证使用合成小样例；尚未用客户真实 BOM 验证业务选型准确率，正式结果仍需人工复核。
