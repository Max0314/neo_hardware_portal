# 硬件研发部系统 — 运维手册

本文档为 **Docker Compose 单机部署** 的日常运维入口，涵盖启停、日志、备份、密码/密钥重置与排障。  
编排入口：根目录 `docker-compose.yml`；环境变量：复制 `.env.example` 为 `.env` 后填写。

**文档导航**（见 [README.md](README.md)）：

| 场景 | 文档 |
|------|------|
| 日常运维 | 本文档 |
| 新服务器联网部署 + 只导入部分数据（如公告栏） | 本文档 §2 + [数据迁移指南.md](数据迁移指南.md) |
| 整机离线迁机 | [迁移手册.md](迁移手册.md) |

---

## 1. 架构速览

| 组件 | 容器名 | 说明 |
|------|--------|------|
| MySQL 8.0 | `stack-mysql` | 用户、公告、积分/看板等业务数据 |
| 管理系统 | `stack-htmlsystm` | Gunicorn，路径 `/` |
| NEO 后端 | `stack-neo-backend` | FastAPI，路径 `/neo/api` |
| NEO 前端 | `stack-neo-web` | 静态 + 容器内反代，路径 `/neo/` |
| 统一网关 | `stack-gateway` | Nginx HTTPS，**唯一对外端口** |

**浏览器访问**（须写 `https://`）：

- 管理系统：`https://<服务器IP>:8000/`
- NEO：`https://<服务器IP>:8000/neo/`
- 端口由 `.env` 中 `GATEWAY_PUBLISH_PORT` 控制（默认 `8000`）

**持久化卷**（前缀 = `COMPOSE_PROJECT_NAME`，例如 `docker_mysql_data`）：

| 卷后缀 | 内容 |
|--------|------|
| `mysql_data` | MySQL 数据文件 |
| `htmlsystm_data` | 公告附件、`quick_links.json` 等 |
| `htmlsystm_uploads` | 上传文件 |
| `ai_chatroom_data` | NEO SQLite、知识库、加密密钥文件 |

**安全操作对照**：

| 操作 | 是否安全 |
|------|----------|
| `docker compose up -d --build` | 安全（保留命名卷） |
| `docker compose down` 后再 `up -d` | 安全（不带 `-v`） |
| `docker compose down -v` | **危险：删除全部卷数据** |

---

## 2. 首次部署

### 2.1 环境准备（Linux）

```bash
docker version
docker compose version
```

当前用户需能访问 Docker（`usermod -aG docker $USER` 后重新登录，或 `newgrp docker`）。

### 2.1.1 北京时间与 NTP 自动对时（推荐首次部署执行）

容器内时间依赖**宿主机**时钟。在 Linux 服务器上执行一次：

```bash
cd /path/to/docker版本
chmod +x migration/setup-timezone.sh
# VirtualBox 共享文件夹（sf_*）下脚本可能是 Windows 换行，须先去掉 CR：
sed -i 's/\r$//' migration/setup-timezone.sh
bash migration/setup-timezone.sh
```

脚本会：`timedatectl set-timezone Asia/Shanghai`、`set-ntp true`（systemd 自动对时）。

`.env` 中保持 `TZ=Asia/Shanghai`（见 `.env.example`）。`docker-compose.yml` 已为全部服务设置 `TZ` 并挂载 `/etc/localtime`，MySQL 使用 `--default-time-zone=+08:00`。

**验证**：

```bash
date                          # 宿主机应为 CST / +0800
docker exec stack-mysql date
docker exec stack-htmlsystm date
docker exec stack-neo-backend date
```

若宿主机无法访问公网 NTP，需在路由器/内网配置 NTP 源，或手动 `sudo timedatectl set-time "YYYY-MM-DD HH:MM:SS"` 后再 `docker compose up -d`。

修改时区配置后重建：

```bash
docker compose up -d --build
```

一键部署（推荐首次执行）：

```bash
bash migration/deploy.sh
```

### 2.2 新服务器部署路径选择

| 场景 | 步骤 | 是否打包镜像 |
|------|------|-------------|
| 联网 + 新代码 + 只导部分数据 | §2.2～2.4 → [数据迁移指南.md](数据迁移指南.md) | 否，`deploy.sh` 现场 build |
| 离线 + 整机原样搬迁 | [迁移手册.md](迁移手册.md) | 是，`run-source-migration.sh` |
| 联网 + 整机搬迁 | 迁移手册 或 deploy + 恢复卷 | 可选 |

### 2.3 配置 `.env`

```bash
cd /path/to/docker版本    # 含 docker-compose.yml 的目录
cp .env.example .env
# 编辑 .env，至少填写以下项
```

| 变量 | 要求 |
|------|------|
| `COMPOSE_PROJECT_NAME` | 固定卷前缀；**已上线勿改名**（生产示例：`docker`）；新装可用 `hwstack`；只导数据时新机可与老机不同 |
| `TZ` | 默认 `Asia/Shanghai`（北京时间） |
| `MYSQL_ROOT_PASSWORD` | 强密码，**仅 ASCII**（勿用中文） |
| `MYSQL_PASSWORD` | 同上，与应用用户一致 |
| `GATEWAY_PUBLISH_PORT` | 对外 HTTPS 端口，默认 `8000` |
| `PUBLIC_BASE_URL` | 小写 `https://IP:8000`，无末尾斜杠 |
| `DEEPSEEK_API_KEY` / `ARK_API_KEY` | NEO AI 密钥（根 `.env`，非子目录） |
| `DINGTALK_CLIENT_SECRET` | 钉钉工作通知（按需） |
| `DINGTALK_SYNC_DEPARTMENT_IDS` | 钉钉用户同步范围：根部门 `dept_id`，多个用英文逗号分隔；不填则默认同步「硬件研发部」及其子部门 |

> API 密钥必须在**根目录** `.env` 填写；`neo_ai_chatroom/.env` 不会打进镜像。

### 2.4 启动（推荐一键部署）

```bash
bash migration/deploy.sh
```

等价于：`docker compose up -d --build` + 等待各容器 healthy + 补建 NEO 表 + `check-db-config.sh` + `check-stack.sh`，并在部署期间写 startup_lock、完成后自动清锁开放登录。

仅重启、不重建镜像：

```bash
bash migration/deploy.sh --no-build
```

仍可使用裸 compose（无自动健康/DB 验收）：

```bash
docker compose up -d --build
docker compose ps
bash migration/check-stack.sh
```

### 2.5 浏览器验收

访问 `https://<局域网IP>:8000/login` 与 `https://<局域网IP>:8000/neo/`。  
自签证书会提示「不安全」，可「高级 → 继续访问」；长期使用见 **第 9 章 TLS**。

---

## 3. 日常启停

```bash
cd /path/to/docker版本

# 日常启动（无代码变更）
docker compose up -d

# 首次部署或代码/Dockerfile 变更（推荐）
bash migration/deploy.sh

# 或裸 compose（需手动自检）
docker compose up -d --build

# 停止（保留数据卷）
docker compose down

# 查看状态
docker compose ps
docker compose ps -a
docker compose config          # 排障：查看解析后的编排
```

**按服务更新**（缩小影响面）：

```bash
docker compose up -d gateway                          # 仅网关/证书/nginx
docker compose up -d --build htmlsystm                # 仅管理系统
docker compose up -d --build backend web              # 仅 NEO
```

**改 `.env` 后**：

- 普通变量（API Key、`PUBLIC_BASE_URL`）：`docker compose up -d` 或重启对应服务
- **MySQL 密码**：仅改 `.env` 不会改卷内密码 → 见 **第 8 章**

**禁止**（除非明确要清空全部数据）：

```bash
docker compose down -v
docker system prune -a    # 弱网下会导致下次重建极慢
```

### 3.1 开机自启（含 stack-gateway）

各服务在 `docker-compose.yml` 中已设 `restart: unless-stopped`，Docker 守护进程重启后会尝试拉起容器。但 **`stack-gateway` 依赖宿主机目录绑定挂载**（`gateway/nginx.conf`、`gateway/certs/`），而 MySQL / htmlsystm 等主要数据在 **命名卷** 里。

若项目放在 **VirtualBox 共享文件夹**（如 `/media/sf_awei/docker版本`），常见现象是：**开机后四个业务容器在跑，唯独 gateway 没有**，`check-stack.sh` 提示未检测到 `stack-gateway`。原因是 Docker 启动往往早于 `vboxsf` 挂载，gateway 首次启动失败后不会自动出现在 `docker compose ps` 的运行列表中。

**立即恢复访问**（在 compose 目录）：

```bash
cd /media/sf_awei/docker版本    # 按实际路径
docker compose up -d gateway
# 或整栈
docker compose up -d
bash migration/check-stack.sh
```

**推荐：安装 systemd 单元**（等 Docker + 共享目录就绪后再 `compose up -d`）：

启动脚本会安装到 **`/usr/local/lib/docker-stack/boot-stack.sh`**（本地盘），避免共享目录未挂载时无法执行 `ExecStartPre`。

```bash
cd /media/sf_awei/docker版本
sed -i 's/\r$//' migration/install-boot-service.sh migration/boot-stack.sh
COMPOSE_USER=zzw sudo bash migration/install-boot-service.sh
```

若系统有用户 `zzw` 且其在 `docker` 组，可省略 `COMPOSE_USER=zzw`。安装脚本会自动等待 `/media/sf_awei` 挂载并拉起 **含 gateway 在内的全部容器**。安装后：

```bash
systemctl status docker-stack.service
journalctl -u docker-stack.service -b --no-pager
```

**长期建议**：将 `docker版本` 目录复制到虚拟机本地盘（如 `/opt/docker-stack`），再改 systemd 中的 `WorkingDirectory`，可减少共享目录权限与挂载顺序问题。

**若曾手动 `docker stop stack-gateway`**：`unless-stopped` 在下次重启 Docker 时也不会拉起该容器，需执行一次 `docker compose up -d gateway` 或依赖上述 systemd 服务。

### 3.2 健康检查与自动恢复

| 机制 | 作用 |
|------|------|
| Compose `healthcheck` | 各服务轻量探针：`/api/health`（htmlsystm）、`/api/health/live`（NEO backend）、`gateway-health` 等 |
| `stack-autoheal` 容器 | 对带标签 `stack.autoheal=true` 的容器，**unhealthy 时自动 restart**（**默认不启动**，compose 使用 `profiles: ["autoheal"]`；需外网拉镜像时 `docker compose --profile autoheal up -d`） |
| `docker-stack-watch.timer`（可选） | 每 3 分钟宿主机巡检：容器未运行 / 连续 unhealthy 时 `compose restart` |
| `docker-stack-maintenance.timer`（可选） | 每天 03:15 删除 **已过期** `sessions` 行，降低登录锁表风险 |

**部署与验收**：

```bash
cd /path/to/docker版本
docker compose up -d --build
bash migration/check-stack.sh    # 查看 1b 节 Health 应为 healthy
curl -sk https://127.0.0.1:8000/api/health
```

**安装宿主机巡检**（与 3.1 开机自启一并配置）：

```bash
INSTALL_WATCH=1 COMPOSE_USER=zzw sudo bash migration/install-boot-service.sh
systemctl status docker-stack-watch.timer
tail -f /var/log/docker-stack-watch.log
```

**可选 `.env` 变量**（见 `.env.example` 末尾注释）：`AUTOHEAL_INTERVAL`、`AUTOHEAL_START_PERIOD`（启动后 300s 内不因探针未就绪而重启）。

**单用户登录拖垮全站**（sessions 堆积）：`bash migration/diagnose-user-login.sh 用户名` → `bash migration/purge-user-sessions.sh 用户名`。

### 3.3 开机启动流水线（进度条 + 就绪后才可登录）

systemd 启动时执行 [`migration/stack-startup.sh`](migration/stack-startup.sh)（安装到 `/usr/local/lib/docker-stack/stack-startup.sh`），在**命令行**显示进度条，依次完成：

1. 等待挂载/Docker → 2. `compose up` → 3. MySQL → 4. NEO 表/库结构 → 5～8. 各服务健康 → 9. HTTPS 自检 → 10. **解除维护锁、开放登录**

维护期间：

- 数据卷内存在 `data/.startup_lock.json` 时，`/api/auth/login` 与 `/api/auth/check` 返回维护提示，**禁止登录**
- 登录页轮询 `GET /api/startup/status` 显示网页进度条
- 完成后写入 `data/.startup_ready` 并删除锁文件

**手动执行（调试）**：

```bash
cd /path/to/docker版本
bash migration/stack-startup.sh
```

**仅 `docker compose up` 而不跑 stack-startup**：无锁文件时登录不受影响（兼容旧用法）。

**开机服务已成功（`docker-stack.service` active）但仍无法登录网页**（与 gateway 无关）：

```bash
cd /media/sf_awei/docker版本
bash migration/diagnose-web-login.sh
docker compose ps    # 须有 stack-gateway 且 8000->443
```

常见原因：① 用了 `http://` 或 Windows 上访问了 `127.0.0.1`（应访问虚拟机 IP）；② 虚拟机 IP 变了与 `.env` 中 `PUBLIC_BASE_URL` 不一致；③ 多次输错密码需**验证码**或 IP 被封禁。清除封禁：

```bash
docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --clear-blocks
docker compose restart htmlsystm
```

忘记密码：`bash migration/fix-mysql-and-admin.sh`

---

## 4. 日志与排障

### 4.1 查看日志

```bash
docker compose logs -f                              # 全栈跟随
docker logs stack-gateway --tail 50
docker logs stack-htmlsystm --tail 80
docker logs stack-neo-backend --tail 80
docker logs stack-neo-web --tail 30
docker logs stack-mysql --tail 30
```

### 4.2 进入容器

```bash
docker compose exec mysql bash
docker compose exec htmlsystm bash
docker compose exec backend bash
```

### 4.3 一键自检

```bash
bash migration/check-stack.sh
```

### 4.4 常见问题

| 现象 | 处理 |
|------|------|
| `required variable MYSQL_PASSWORD is missing` | 根目录 `.env` 未配置或为空 |
| `latin-1 codec can't encode` / 连不上库 | `MYSQL_PASSWORD` 含中文；改为 ASCII，并用 `fix-mysql-and-admin.sh` 对齐 |
| `permission denied ... docker.sock` | 用户加入 `docker` 组后重新登录 |
| `stack-gateway` Restarting | `docker logs stack-gateway`；检查 `gateway/nginx.conf` 与证书文件 |
| **开机后**其它容器在跑、无 `stack-gateway` | 共享目录挂载晚于 Docker → **3.1 节**；临时 `docker compose up -d gateway` |
| 登录页/接口 JSON 为 `detail: Not Found` | 登录 API 误进 NEO（FastAPI）；`docker compose up -d --force-recreate gateway`；`check-stack.sh` 第 5b 项应含 `"success"` |
| 浏览器「无法连接」/ `127.0.0.1:8000` 拒绝 | 须在含 `docker-compose.yml` 的目录执行 `docker compose up`；`docker compose ps` 中须有 `stack-gateway` 且 `PORTS` 含 `0.0.0.0:8000->443`；访问用 **`https://`** 非 `http://` |
| **经常**连接失败 / 「太过忙碌」 | 见下方 **4.5**；常见：用了 `http://`、在 Windows 上访问 `127.0.0.1` 但 Docker 在 Linux 服务器上、`stack-htmlsystm` 重启或 unhealthy |
| `web` 构建 `npm ci` / `package-lock` 不同步 | 在 `neo_ai_chatroom` 执行 `npm install` 更新锁文件后 `docker compose up -d --build`；或拉取已修复的 `package-lock.json` |
| 浏览器白屏 | 勿用 `http://`；须 `https://IP:8000`；会话过期后应自动跳转 `/login?redirect=...`（非白屏）。若仍白屏：`docker compose up -d --build web` 重建 NEO |
| 登录一直「登录中」/ 按钮无响应 | 多为 IP 封禁或需验证码：`docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --clear-blocks` 后 `docker compose restart htmlsystm`；登录页须填写验证码；密码错误用 `fix-mysql-and-admin.sh` |
| **`504 Gateway Timeout`** / `auth/check` 120s / `NetworkError` | **`stack-htmlsystm` unhealthy 或 worker 排队**；`.env` 设 `SKIP_DINGTALK_SYNC_ON_START=1`、`SKIP_QUICK_LINK_ICON_FETCH_ON_START=1` 后 `docker compose up -d --build --force-recreate htmlsystm`；看 `docker logs stack-htmlsystm --tail 100` |
| **`favicon.ico` 404/403** / 控制台仅图标报错 | gateway 已将 favicon **反代到 htmlsystm**（勿用本地 alias 读共享文件夹，易 Permission denied→403）；改 `proxy_locations.conf` 后须 **`docker compose up -d --force-recreate gateway`**；验证 `curl -skI https://127.0.0.1:8000/favicon.ico` 应为 **200** + `image/svg+xml` |
| 修改 **`gateway/nginx.conf`** 或 **`proxy_locations.conf`** | 配置挂载仅在容器**创建**时生效，须 **`docker compose up -d --force-recreate gateway`**（不是仅 `restart`） |
| 日志有 **用户认证成功** 但无 **登录成功** / 一直登录中 | 卡在写 `sessions` 或 `login_attempts` 表；`docker compose up -d --build htmlsystm` 应用最新代码；或查账号 `status` 是否为 `active` |
| NEO 与登录页来回闪烁 | 多为单次 API 401 误踢 + 自动登录重复 POST；更新代码后 `docker compose up -d --build web backend htmlsystm`；退出后勿自动登录；改密后该用户须重新登录 |
| **普通用户登录成功立刻回登录页** / `leaderboard` **401** / neo 日志 `[auth] htmlsystm 校验失败 (TimeoutError)` | 一键：`bash migration/fix-login-full.sh`（或带新密码 `bash migration/fix-login-full.sh 'YourPass'`）；查会话：`bash migration/verify-login-sessions.sh`；重建：`docker compose up -d --build --force-recreate htmlsystm backend gateway`；改密后浏览器须清 Cookie、关自动登录、用新密码 |
| **全员无法登录** / 管理员刚批量改密 | `bash migration/fix-login-full.sh '新密码'` → 各客户端清 Cookie、关自动登录、勿用 localStorage 旧密码 |
| **某用户一登录全站卡死/崩溃** | `bash migration/diagnose-user-login.sh 用户名`；`session_rows` 很大时 `bash migration/purge-user-sessions.sh 用户名`；`docker compose up -d --build --force-recreate htmlsystm backend gateway` |
| 管理员改用户密码后异常 | 改密会清除**该用户**全部会话；浏览器若仍用旧密码「自动登录」会失败并闪烁，应取消自动登录并用新密码登录 |
| 改某用户密码后**全站卡顿** / neo `TimeoutError` | 须 **`docker compose up -d --build --force-recreate htmlsystm backend`**（仅 `Running` 未 Recreate 时 backend 仍是旧代码）；改密后等 3～5 秒再登录；NEO 现走 `/api/internal/neo/verify-session` 轻量校验 |
| 岗位身份 | 仅 **普通用户**（`user`）与 **管理者**（`manager`）；系统权限（admin/management）仍在账号管理单独勾选 |
| `set: pipefail` 无效选项 / 第 4 行报错 | 共享文件夹脚本为 Windows 换行（CRLF）：`sed -i 's/\r$//' migration/ensure-neo-mysql-tables.sh` 后再执行；或 `sed -i 's/\r$//' migration/*.sh` |
| `neo_point_events doesn't exist` | 老库未建 NEO 表：`bash migration/ensure-neo-mysql-tables.sh` 后 `docker compose restart backend` |
| 命令 `cd /path/to/docker版本` 失败 | 文档占位符；请 `cd` 到实际目录，如 `/media/sf_awei/docker版本` |
| 查看日志 | 用 `docker logs stack-htmlsystm --tail 50`（不是 `docker log`） |
| `curl` 登录页乱码 | 正常：响应为 gzip 压缩；用 `curl -skI https://127.0.0.1:8000/login` 看 HTTP 200 即可 |
| 自检缺 NEO 表 | `bash migration/ensure-neo-mysql-tables.sh`；`check-stack.sh` 默认会自动补建 |
| IP 已被封禁 / 验证码错误 | 见上 `clear-blocks`；日志含 `IP ... 被加入黑名单`；封禁期间勿反复点登录 |
| `stack-htmlsystm` unhealthy | `docker logs stack-htmlsystm --tail 80`；常见：MySQL 密码不一致、钉钉全量同步阻塞（保持 `SKIP_DINGTALK_SYNC_ON_START=1`） |
| 容器名冲突 | `docker inspect stack-mysql` 看 `project=`；`.env` 中 `COMPOSE_PROJECT_NAME` 须与旧卷一致 |
| 积分/看板归零 | `docker volume ls` 检查是否误挂到新空卷；恢复正确的 `COMPOSE_PROJECT_NAME` |

### 4.5 浏览器「连接到 127.0.0.1:8000 时发生错误」/ 经常连不上

**正确访问方式**（缺一不可）：

1. 地址必须是 **`https://`**，不能是 `http://`。网关容器内是 TLS 443，映射到宿主机 8000；用 HTTP 访问会握手失败，Firefox 显示「无法连接」或「太过忙碌」。
2. 在**运行 Docker 的那台机器**上本机访问才用 `https://127.0.0.1:8000`。若 Docker 在 Linux 服务器（如 `10.70.33.26`），在 Windows 办公电脑上应访问 **`https://10.70.33.26:8000`**，不要用 `127.0.0.1`（那是你本机，不是服务器）。
3. 首次访问自签证书：在 Firefox 地址栏点「高级」→ 接受风险并继续。

**在服务器上快速自检**（SSH 到部署机执行）：

```bash
cd /home/chxw/docker版本   # 或你的 compose 目录
docker compose ps
curl -skI https://127.0.0.1:8000/gateway-health | head -3
curl -skI https://127.0.0.1:8000/login | head -5
docker logs stack-gateway --tail 30
docker logs stack-htmlsystm --tail 50
```

**判断**：

| `docker compose ps` | 含义 |
|---------------------|------|
| `stack-gateway` 无 `8000->443` 或 Exited | 执行 `docker compose up -d` |
| `stack-htmlsystm` **unhealthy** 或 Restarting | 看 `docker logs stack-htmlsystm`；保持 `.env` 中 `SKIP_DINGTALK_SYNC_ON_START=1` |
| 网关 healthy 但 `/login` 502 | 后端未就绪，等 1～2 分钟或 `docker compose restart htmlsystm` |

**间歇性失败**常见原因：`stack-htmlsystm` 内存不足被 OOM 重启（小设备上已限制 2 worker）；审批/钉钉长请求占满 worker 时短暂 502。处理：`docker compose restart htmlsystm gateway`，并确认 `.env` 已配置 `SKIP_DINGTALK_SYNC_ON_START=1`。

更新网关配置后：`docker compose up -d gateway`（或 `docker compose restart gateway`）。

---

## 5. 健康检查

```bash
# 网关存活（不依赖业务后端）
curl -skI https://127.0.0.1:8000/gateway-health | head -3

# HTTPS 登录页与 NEO
curl -skI https://127.0.0.1:8000/login | head -5
curl -skI https://127.0.0.1:8000/neo/ | head -5

# 鉴权 API
curl -sk https://127.0.0.1:8000/api/auth/check

# NEO 持久化（积分是否在 MySQL）
curl -sk https://127.0.0.1:8000/neo/api/health/persistence | python3 -m json.tool
```

**判断标准**：

- 五个容器均为 `running`（`htmlsystm` 须 `healthy`）
- persistence 接口 `storage` 为 `mysql`；若已有积分，`pointEventsCount > 0`

---

## 6. 备份

### 6.1 日常在线备份（推荐每周 / 发布前）

**Linux**：

```bash
cd /path/to/docker版本
chmod +x scripts/backup-all.sh
./scripts/backup-all.sh
# 指定目录：./scripts/backup-all.sh /data/backups/hwstack_20260518
```

**Windows**：

```powershell
cd Z:\docker版本
.\scripts\backup-all.ps1
# 指定目录：.\scripts\backup-all.ps1 -BackupDir "D:\backups\hwstack_20260518"
```

**备份产物**（目录 `backup/backup_YYYYMMDD_HHMMSS/`）：

- `mysql_YYYYMMDD_HHMMSS.sql`（用户、公告、积分/看板等）
- `mysql_data.tar.gz`、`htmlsystm_data.tar.gz`、`htmlsystm_uploads.tar.gz`、`ai_chatroom_data.tar.gz`
- `env.snapshot`（含密钥，妥善保管）、`manifest.txt`

### 6.2 定时备份（Linux cron 示例）

```bash
# 每周日 02:00
0 2 * * 0 cd /path/to/docker版本 && ./scripts/backup-all.sh /data/backups/weekly
```

### 6.3 备份策略建议

- 每周 1 次自动备份 + 发布前手动备份
- 保留最近 4 周 + 每月 1 份长期归档
- 复制到 U 盘 / NAS / 另一台机器（3-2-1 原则）

### 6.4 仅导出公告栏（迁到新服务器）

若只需迁移公告栏数据（不含用户/积分），在老生产机执行：

```bash
./scripts/export-announcements.sh
```

新机 deploy 后执行 `./scripts/import-announcements.sh <备份目录>`。详见 [数据迁移指南.md](数据迁移指南.md)。

---

## 7. 恢复与迁机

### 7.1 从日常备份恢复（单卷 + SQL）

```bash
cd /path/to/docker版本
BACKUP=backup/backup_YYYYMMDD_HHMMSS
PREFIX=docker    # 与 COMPOSE_PROJECT_NAME 一致

# 1. 停栈
docker compose stop

# 2. 恢复某卷（示例：mysql_data）
docker run --rm \
  -v ${PREFIX}_mysql_data:/data \
  -v $(pwd)/${BACKUP}:/backup:ro \
  alpine:3.19 sh -c "cd /data && tar xzf /backup/mysql_data.tar.gz"

# 3. 恢复 MySQL 逻辑备份（可选，与卷恢复二选一或互补）
docker compose start mysql
sleep 10
docker compose exec -T mysql sh -c \
  'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' \
  < ${BACKUP}/mysql_YYYYMMDD_HHMMSS.sql

# 4. 启动全栈并自检
docker compose up -d
bash migration/check-stack.sh
```

其他卷将 `mysql_data` 替换为 `htmlsystm_data`、`htmlsystm_uploads`、`ai_chatroom_data`。

### 7.2 整机离线迁机

源机打包、目标机恢复、分步脚本说明见 **[迁移手册.md](迁移手册.md)**（勿与本章 §7.1 日常备份恢复混淆）。

简要命令：

```bash
# 源机
./migration/run-source-migration.sh
# 栈已在跑、不想重复下载：SKIP_BUILD=1 SKIP_PULL=1 ./migration/run-source-migration.sh

# 目标机（编辑 .env 后）
./migration/run-target-migration.sh /path/to/migration_backup_YYYYMMDD_HHMMSS
```

---

## 8. 密码与密钥重置

### 8.0 整站无法访问 / 容器已停（紧急）

**现象**：`docker exec stack-htmlsystm` 报 `Container is not running`；`fix-mysql-and-admin.sh` 因拉取 `autoheal` 镜像超时失败。

```bash
cd /media/sf_awei/docker版本
sed -i 's/\r$//' migration/*.sh
# 只拉起核心服务 + 重置 zzw（不重置 MySQL 卷、不拉 autoheal）
bash migration/emergency-recover.sh 'YourSecurePass123!'
```

仅恢复访问、不改密码：`bash migration/emergency-recover.sh --skip-password`

若 `stack-neo-backend` 为 unhealthy 导致 gateway 起不来，新版 `emergency-recover.sh` 会用 `docker-compose.emergency.yml` 仅依赖 htmlsystm 启动网关；NEO 可稍后修复：`bash migration/diagnose-neo-backend.sh`

手动仅开登录入口：

```bash
docker compose up -d mysql htmlsystm
docker compose -f docker-compose.yml -f docker-compose.emergency.yml up -d gateway
```

若 `--reset` 显示「数据库已更新但 authenticate_user 仍失败」，多为旧镜像登录路径误删了 `password` 字段；`docker compose up -d --build --force-recreate htmlsystm` 后重新执行 reset，或用：

```bash
docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --diagnose zzw --password 'YourSecurePass123!'
```

### 8.1 MySQL 密码与 `.env` 不一致

卷只在**首次创建**时写入密码；之后只改 `.env` 不会自动改卷内密码。

```bash
# 仅对齐 MySQL（不删卷）
bash migration/reset-mysql-password.sh

# 改 .env 中 MYSQL_PASSWORD 后一键修复（对齐卷密码 + 重启服务 + 清启动锁 + 验收）
bash migration/fix-mysql-env-password.sh

# MySQL + 管理员 zzw 一键修复（推荐；密码必填，不再读 .env）
bash migration/fix-mysql-and-admin.sh 'YourSecurePass123!'

# 查看当前 zzw 密码（数据卷内 admin_credentials.json）
docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --show-admin

# 检查 MySQL 与登录凭据是否一致
bash migration/check-db-config.sh
```

**说明**：`MYSQL_PASSWORD` 是应用连库密码；`zzw` 登录密码保存在 `htmlsystm_data` 卷的 `data/admin_credentials.json`，**首次全新安装**时随机生成并打印到 `docker compose logs htmlsystm`。`.env` 中的 `SUPER_ADMIN_PASSWORD` 已废弃，请从 `.env` 删除该行。

### 8.2 重建用户库（仅保留 zzw，手动再同步钉钉）

**适用**：单个用户（如 `20461992`）数据异常、需清空全部钉钉用户后重新同步。

**不会删除**：公告栏、物料库配置、`system_config`、管理员 `zzw`。  
**会删除**：其他 `users`、全部 `sessions`、待办 `todos`、NEO 积分/看板 MySQL 表、登录封禁记录。

```bash
cd /media/sf_awei/docker版本
sed -i 's/\r$//' migration/*.sh

# 1. 预览影响（不改动）
bash migration/reset-users-keep-zzw.sh --dry-run

# 2. 确认执行（自动备份 users 相关表到 migration/backups/）
bash migration/reset-users-keep-zzw.sh --confirm

# 3. 可选：同时重置 zzw 登录密码
bash migration/reset-users-keep-zzw.sh --confirm 'YourSecurePass123!'

# 4. 登录 zzw → 管理界面手动「同步钉钉用户」
docker compose restart htmlsystm
```

**同步部门范围**：在 `.env` 中设置 `DINGTALK_SYNC_DEPARTMENT_IDS`（钉钉根部门 `dept_id`，多个用英文逗号分隔，会包含各根部门下所有子部门）。不填则默认按名称同步「硬件研发部」。`dept_id` 可在管理界面「钉钉部门列表」测试页查看，或从 `docker logs stack-htmlsystm` 同步日志中获取。修改后执行 `docker compose up -d --force-recreate htmlsystm` 使配置生效。

**MySQL 注意**：脚本通过 `stack-mysql` 容器内 `MYSQL_USER`/`MYSQL_PASSWORD` 操作，与 `.env` 一致；**不要**在此流程中运行 `reset-mysql-password.sh`（会停栈）。

若 NEO 仍显示旧积分，且环境使用 SQLite 看板库，可额外：

```bash
docker exec stack-neo-backend rm -f /data/dashboard_metrics.db 2>/dev/null || true
docker compose restart backend
```

### 8.3 重置普通用户密码

```bash
docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --reset 用户名 --password 'YourSecurePass123!'
docker exec stack-htmlsystm python scripts/view_and_reset_admin_password.py --clear-blocks
docker compose restart htmlsystm
```

### 8.4 更新 API 密钥 / 钉钉密钥

编辑根目录 `.env` 后：

```bash
docker compose up -d htmlsystm backend
```

### 8.5 `.env` 关键检查项

- `MYSQL_PASSWORD` 必须纯 ASCII
- `PUBLIC_BASE_URL` 小写 `https://`，无末尾斜杠
- `COMPOSE_PROJECT_NAME` 与现有卷前缀一致
- 保持 `SKIP_DINGTALK_SYNC_ON_START=1`（避免启动 healthcheck 超时）

---

## 9. TLS 证书

证书目录 `gateway/certs/`（挂载到 Nginx `/etc/nginx/certs/`）：

| 文件 | 说明 |
|------|------|
| `server.crt` | 证书（PEM） |
| `server.key` | 私钥（勿泄露） |

**为当前 IP 生成 SAN 自签证书**：

```bash
bash migration/gen-gateway-cert.sh 192.168.1.77
docker compose up -d gateway
```

**Windows 客户端信任自签证书**：

1. 复制 `gateway/certs/server.crt` 到 Windows
2. 双击 → 安装证书 → 受信任的根证书颁发机构
3. 重启浏览器访问 `https://IP:8000`

**生产/公网**：使用正式 CA 或企业 PKI 签发，替换 `server.crt` / `server.key` 后 `docker compose up -d gateway`。

---

## 10. 安全发布流程

```
备份 → 同步代码 → bash migration/deploy.sh → 浏览器冒烟
```

| 步骤 | 命令 |
|------|------|
| 1. 备份 | `./scripts/backup-all.sh` |
| 2. 构建发布 + 自检 | `bash migration/deploy.sh` |
| 3. 冒烟 | 登录 `/login`、访问 `/neo/`、确认历史数据 |

---

## 11. 周期性运维任务

| 周期 | 任务 |
|------|------|
| 每日 | `docker compose ps`；异常则查对应 `docker logs` |
| 每周 | `backup-all`；`df -h` + `docker system df`；`health/persistence` |
| 每月 | 备份异地拷贝；检查 Docker/Compose 版本；复查密钥是否轮换 |
| 每季度 | 演练从备份恢复或目标机迁机 |

**变更台账**（建议记录）：

| 日期 | 操作 | 备份路径 | 结果 |
|------|------|---------|------|
| 示例 | 更新 NEO 后端 | backup/backup_... | 自检通过 |

---

## 12. 安全附录（宿主机）

### 12.1 HTTPS 复测要点

```bash
curl -skI "https://<host>:8000/login" | grep -iE 'strict-transport|content-security|x-frame'
```

部署后验证：账号密码登录、钉钉免登、NEO `/neo/` 与 WebSocket、公告附件上传白名单、CSRF 防护。

### 12.2 长时间无操作、断网与会话失效

- **默认**：连续 **30 分钟** 无鼠标/键盘/滚动/触摸操作，或标签页自隐藏起超过 30 分钟再切回，触发处理。
- **管理系统**（`/`、公告、用户管理等）：
  - 注入 `session-guard.js`：页面加载、`online` 事件、长时间隐藏后切回时调用 `/api/auth/check`，未登录 → `/login?redirect=当前路径`（**不再**用 `sessionStorage` 缓存冒充已登录）。
  - `idle-guard.js`：空闲后先验会话；失效跳登录；断网时不误踢，恢复 `online` 后再验。
- **NEO**（`/neo/*`）：
  - `AuthGate` 进入任意子路由前调用 `/api/auth/me`，未登录 → `/login?redirect=/neo/...`。
  - 空闲：子页回 `/neo/`；主页且会话有效时刷新 UI；会话失效跳登录（避免无效刷新白屏）。
  - 渲染异常时 `NeoErrorBoundary` 提供「返回登录」。
- **钉钉工作通知**：链接为 `https://<PUBLIC_BASE_URL>/announcement-detail/<id>`，未登录会先到登录页，登录成功后按 `redirect` 进入对应公告详情（`.env` 中 `PUBLIC_BASE_URL` 须与浏览器访问地址一致）。
- **调整 NEO 超时**：构建前在 `neo_ai_chatroom/.env` 设置 `VITE_IDLE_TIMEOUT_MINUTES=45`（单位：分钟，最小 5），然后 `docker compose up -d --build web`。

### 12.3 BOM 对比工具（`/neo/bom-compare`）

- **访问路径**：`https://<host>:8000/neo/bom-compare`（须经 gateway，勿直连 `web` 容器端口）。
- **模式**：页内切换三种 iframe 工具——**全部对比**（`bom-compare.html`）、**仅位号对比**（`refdes-compare.html`）、**坐标与封装**（`coord-bom-package.html`）。
- **多人使用**：对比在浏览器本地完成（Excel 不上传服务器），无单用户锁；多人可同时打开，互不影响。完成对比后 iframe 会向父页面上报积分（`neo_compare_tool_done`）。
- **资源**：SheetJS、Font Awesome 已自托管于 `/neo/vendor/`，CSP 仅需 `style-src 'self'` / `script-src 'self'`，不依赖 Google Fonts 或外网 CDN。
- **更新后重建**（修改 `neo_ai_chatroom/BOM_TOOL` 或前端后必做）：

```bash
# 必须显式 build web；仅 docker compose up 不会更新已 Running 的旧镜像
docker compose build web --no-cache
docker compose up -d --force-recreate web gateway
```

若 `web` 构建报错 `the --mount option requires BuildKit`：已改为普通 `RUN npm ci`（见 `neo_ai_chatroom/Dockerfile.web`）；请重新拉取代码后再执行上条命令。也可启用 BuildKit：`export DOCKER_BUILDKIT=1` 后重建。

- **部署后自检**（在宿主机执行，任一条无输出则说明仍是旧 `web` 镜像）：

```bash
docker exec stack-neo-web ls -la /usr/share/nginx/html/bom_tool/
docker exec stack-neo-web test -f /usr/share/nginx/html/bom_tool/coord-bom-package.html && echo OK_coord_html
docker exec stack-neo-web sh -c "grep -l coordPackage /usr/share/nginx/html/assets/*.js 2>/dev/null | head -1"
```

浏览器打开 `/neo/bom-compare` 应看到第三个按钮 **坐标与封装**；iframe 地址为 `/neo/bom_tool/coord-bom-package.html`。

- **CSP 自检**（应含 `frame-ancestors 'self'`、`X-Frame-Options: SAMEORIGIN`）：

```bash
curl -skI "https://127.0.0.1:8000/neo/bom-compare" | grep -iE 'content-security|x-frame'
curl -skI "https://127.0.0.1:8000/neo/bom_tool/coord-bom-package.html" | head -1
```

- 浏览器控制台中的 `dsp.lenovo.com.cn`、翻译插件 `content.js` 报错可忽略，与系统无关。

### 12.4 原理图 AI 审核（`/neo/meeting`）

- **入口**：NEO 主页卡片「原理图AI审核」，路径 `/neo/meeting`（`mode=schematic`）。
- **流程**：Step1 导入网表（`.txt`/`.asc`）→ Step2 网表清洗 → Step3 AI 评审与报告（PASS/WARNING/INFO）→ Step4 导出 PDF（**+1 积分**，事件 `schematic_review_export`）→ Step5 导出后可选与 AI 对话（带评审上下文、支持 txt/pdf/docx 等附件）。
- **权限**：`management` / `admin` / `super_admin` 可配置 API 密钥；普通用户仅选用模型。
- **说明**：原 AI 工作室、SOP、事件流、巴巴塔已从该入口移除；`/neo/bom-check` 不受影响。

### 12.5 SSH 加固要点

在部署服务器上执行（非容器内）：

```bash
sudo apt update && sudo apt install --only-upgrade openssh-server
```

`sshd_config` 建议：`PermitRootLogin no`、`PasswordAuthentication no`（已用密钥时）、`MaxAuthTries 3`。  
修改后：`sudo sshd -t && sudo systemctl reload sshd`。

### 12.6 数据层注意

- `PUBLIC_BASE_URL` 使用对外 HTTPS 地址，避免公告/快捷链接硬编码内网 IP
- 上传目录勿放入可执行文件

---

## 13. 命令速查表

```bash
# === 环境 ===
cd /path/to/docker版本

# === 启停 ===
docker compose up -d
docker compose up -d --build
docker compose down
docker compose ps

# === 日志 ===
docker compose logs -f
docker logs stack-gateway --tail 50
docker logs stack-htmlsystm --tail 80
docker logs stack-neo-backend --tail 80

# === 自检 ===
bash migration/check-stack.sh
curl -skI https://127.0.0.1:8000/login | head -5
curl -skI https://127.0.0.1:8000/favicon.ico | head -5
curl -sk https://127.0.0.1:8000/api/health
curl -sk https://127.0.0.1:8000/neo/api/health/persistence

# === 登录架构（签名 Cookie）部署 ===
# .env 建议设置独立密钥（32+ 字符）；未设置则回退 NEO_INTERNAL_SECRET
# AUTH_SESSION_SECRET=随机长串
# MAX_SESSIONS_PER_USER=10
docker compose build --no-cache htmlsystm
docker compose up -d --force-recreate htmlsystm gateway

# === 登录/退出循环测试（密码勿写入仓库，用环境变量） ===
export AUTH_TEST_PASSWORD='你的密码'
# 推荐：容器内直连，绕过共享目录 CRLF 与网关 TLS
bash migration/test-auth-logout-loop.sh --in-container --user 20461992 --rounds 3
# 共享目录 bash 仍报错时，一条命令搞定（不依赖 .sh）:
# sed 's/\r$//' migration/test-auth-logout-loop.py > /tmp/t.py && docker cp /tmp/t.py stack-htmlsystm:/tmp/t.py && docker exec -e AUTH_TEST_PASSWORD="$AUTH_TEST_PASSWORD" stack-htmlsystm python3 /tmp/t.py --base http://127.0.0.1:8000 --user 20461992 --rounds 3
# 本机经网关（默认 https://127.0.0.1:8000，勿用 PUBLIC_BASE_URL）:
# python3 migration/test-auth-logout-loop.py --user 20461992 --rounds 3

# === NetworkError / 504 / favicon / 登录重构后应用代码 ===
docker compose build --no-cache htmlsystm
docker compose up -d --force-recreate htmlsystm
docker compose up -d --force-recreate gateway
# 确认 gateway 已加载新 favicon 规则（应为 proxy_pass，勿仍是 alias）：
docker exec stack-gateway grep -A2 'favicon.ico' /etc/nginx/proxy_locations.conf
curl -skI https://127.0.0.1:8000/favicon.ico | head -5

# === 备份 ===
./scripts/backup-all.sh

# === 公告栏导出/导入（见 数据迁移指南.md）===
./scripts/export-announcements.sh
./scripts/import-announcements.sh /path/to/announce_export_*

# === 迁机（整机离线，见 迁移手册.md）===
./migration/run-source-migration.sh
./migration/run-target-migration.sh /path/to/migration_backup_*

# === 密码 ===
bash migration/fix-mysql-env-password.sh
bash migration/fix-mysql-and-admin.sh 'YourSecurePass123!'
bash migration/reset-mysql-password.sh

# === 证书 ===
bash migration/gen-gateway-cert.sh <IP>
docker compose up -d gateway

# === 卷核对 ===
docker volume ls | grep docker_
docker inspect stack-mysql --format '{{index .Config.Labels "com.docker.compose.project"}}'

# === 镜像维护（谨慎）===
docker system df
docker system prune
```

---

## 14. 相关文件索引

| 路径 | 作用 |
|------|------|
| `README.md` | 文档入口与场景导航 |
| `docker-compose.yml` | 编排入口 |
| `.env.example` | 环境变量模板 |
| `scripts/backup-all.sh` / `.ps1` | 日常备份 |
| `scripts/export-announcements.sh` | 公告栏导出 |
| `scripts/import-announcements.sh` | 公告栏导入 |
| `数据迁移指南.md` | 选择性数据迁移 |
| `迁移手册.md` | 整机离线迁机 |
| `migration/` | 迁机、自检、密码修复、证书生成、`setup-timezone.sh`、`install-boot-service.sh` |
| `migration/systemd/docker-stack.service` | 开机自启单元模板 |
| `migration/boot-stack.sh` | 开机等待挂载/Docker 并 `compose up -d`（安装到 `/usr/local/lib/docker-stack/`） |
| `gateway/nginx.conf` | 网关配置 |
| `htmlsystm/Dockerfile` | 管理系统镜像 |
| `neo_ai_chatroom/Dockerfile.backend` / `Dockerfile.web` | NEO 镜像 |

---

*文档随部署实践更新；变更端口或服务名时请同步修改 `.env.example` 与本手册。*
