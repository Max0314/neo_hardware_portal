# 数据迁移指南

本文档说明如何**选择性**导出、导入业务数据（公告栏、用户等），适用于「新服务器安装当前代码 + 从老环境导入部分数据」的场景。

- 日常运维见 [运维手册.md](运维手册.md)
- **整机离线迁机**（镜像+四卷+配置原样搬迁）见 [迁移手册.md](迁移手册.md)，勿与本指南混淆

---

## 1. 公告栏数据构成

公告栏采用**文件 + MySQL 混合存储**，二者须同时迁移：

| 存储位置 | 内容 |
|----------|------|
| Docker 卷 `{PREFIX}_htmlsystm_data` 内 `announcements/` | 正文 HTML、附件、历史版本、草稿 |
| MySQL 表 `primary_boards` | 一级公告栏分类 |
| MySQL 表 `sub_boards` | 二级公告栏分类 |
| MySQL 表 `todos` | 阅读待办 |

正文**不在** MySQL 中；只导 SQL 或只拷文件都会导致公告栏不完整。

---

## 2. 推荐路径：新服务器 + 只迁公告栏

### 2.1 老生产环境 — 导出

**方式 A：一键脚本（推荐）**

```bash
cd /path/to/docker版本
chmod +x scripts/export-announcements.sh
./scripts/export-announcements.sh
# 或指定目录：./scripts/export-announcements.sh /data/backups/announce_export_20260529
```

产出目录示例 `backup/announce_export_YYYYMMDD_HHMMSS/`：

| 文件 | 说明 |
|------|------|
| `announcements_only.tar.gz` | 完整 `announcements/` 目录 |
| `announcement_meta.sql` | `primary_boards`、`sub_boards`、`todos` |
| `manifest.txt` | 导出清单 |

**方式 B：统一备份（含全部业务数据）**

```bash
./scripts/backup-all.sh /data/backups/full_backup_$(date +%Y%m%d)
```

取 `htmlsystm_data.tar.gz` + `mysql_*.sql`；导入时 `import-announcements.sh` 也支持这两种文件。

将产出目录拷贝到 U 盘 / NAS / 新服务器。

### 2.2 新服务器 — 全新部署

1. 拷贝整个 `docker版本/` 项目（**不要**拷贝老机 `migration_backup_*` 镜像包）
2. `cp .env.example .env`，填写新 IP、`PUBLIC_BASE_URL`、密码与 API Key
3. **`COMPOSE_PROJECT_NAME` 可与老环境不同**（如老机 `docker`、新机 `hwstack`）— 导入脚本按新机 `.env` 操作卷
4. 生成证书并部署：

```bash
bash migration/gen-gateway-cert.sh <新服务器IP>
bash migration/deploy.sh
```

5. 验收 `https://<新IP>:8000/login` 可访问（此时公告栏为空属正常）

### 2.3 新服务器 — 导入公告栏

在 **deploy 成功后**执行：

```bash
cd /path/to/docker版本
chmod +x scripts/import-announcements.sh
./scripts/import-announcements.sh /path/to/announce_export_YYYYMMDD_HHMMSS
```

脚本会：停 `htmlsystm` → 恢复 `announcements/` → 清空并导入公告相关 MySQL 表 → 启动全栈 → `check-stack.sh`。

**验收清单**：

- 登录后公告栏分类与老环境一致
- 随机打开 2～3 篇已发布公告，正文与附件正常
- 阅读待办数量合理

**注意**：

- 用户账号**不在**公告导出范围内；新环境需重新建用户或「同步钉钉用户」
- 若使用完整 `mysql_*.sql`（`backup-all` 产出）导入，会覆盖用户/积分等**全部**表；仅迁公告时请用 `export-announcements.sh` 产出的小文件

---

## 3. 手工命令（脚本等价操作）

### 导出（老机）

```bash
PREFIX=docker   # 与 .env 中 COMPOSE_PROJECT_NAME 一致
docker run --rm \
  -v ${PREFIX}_htmlsystm_data:/data:ro \
  -v $(pwd):/backup \
  alpine:3.19 tar czf /backup/announcements_only.tar.gz -C /data announcements

docker compose exec -T mysql sh -c \
  'mysqldump -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE" primary_boards sub_boards todos' \
  > announcement_meta.sql
```

### 导入（新机）

```bash
PREFIX=hwstack   # 新机 .env 中的 COMPOSE_PROJECT_NAME
BACKUP=/path/to/announce_export

docker compose stop htmlsystm
docker run --rm \
  -v ${PREFIX}_htmlsystm_data:/data \
  -v ${BACKUP}:/backup:ro \
  alpine:3.19 sh -c 'cd /data && rm -rf announcements && tar xzf /backup/announcements_only.tar.gz announcements'

docker compose start mysql && sleep 10
docker compose exec -T mysql sh -c \
  'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE" -e "SET FOREIGN_KEY_CHECKS=0; TRUNCATE todos; TRUNCATE sub_boards; TRUNCATE primary_boards; SET FOREIGN_KEY_CHECKS=1;"'
docker compose exec -T mysql sh -c \
  'mysql -u"$MYSQL_USER" -p"$MYSQL_PASSWORD" "$MYSQL_DATABASE"' < ${BACKUP}/announcement_meta.sql

docker compose up -d
bash migration/check-stack.sh
```

---

## 4. 其他数据迁移（简要）

| 数据 | 位置 | 迁移方式 |
|------|------|----------|
| 用户 / 积分 / 物料 | MySQL 各表 | `backup-all.sh` 的 `mysql_*.sql` 或整机迁机 |
| 上传文件 | `{PREFIX}_htmlsystm_uploads` | `backup-all` 的 `htmlsystm_uploads.tar.gz` |
| NEO 知识库 / 密钥 | `{PREFIX}_ai_chatroom_data` | `backup-all` 的 `ai_chatroom_data.tar.gz` |
| 快捷链接等 | `htmlsystm_data` 卷根目录 | 随 `htmlsystm_data.tar.gz` 或单独拷贝 `quick_links.json` |

整机搬迁上述全部数据请使用 [迁移手册.md](迁移手册.md)。

---

## 5. 风险与注意

- **禁止** `docker compose down -v`（会删除全部卷数据）
- 附件路径与公告 UUID 绑定，必须整目录迁移 `announcements/`
- 老环境 `COMPOSE_PROJECT_NAME=docker` 时卷名为 `docker_htmlsystm_data`；新机可用不同前缀，导入时按新机 `.env` 即可
- 导入前建议对新环境做一次 `backup-all.sh` 留底
